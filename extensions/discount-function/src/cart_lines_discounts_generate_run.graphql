# [START discount-function.graphql.cart]
query CartInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        __typename
        ... on ProductVariant {
          product {
            id
          }
        }
      }
    }
  }
  shop {
    metafield(namespace: "smart_bundle", key: "active_bundles") {
      jsonValue
    }
  }
}
# [END discount-function.graphql.cart]
